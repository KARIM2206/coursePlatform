'use client'
import React from 'react'

const PlaylistQuizList = ({ quizzes, locale, courseId, playlistId }) => {
    console.log(quizzes);
    
  return (
    <div>
      quizzes
    </div>
  )
}

export default PlaylistQuizList
