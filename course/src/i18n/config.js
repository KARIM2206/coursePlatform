// Synchronous dictionary imports
import en from './dictionaries/en.json';
import ar from './dictionaries/ar.json';

export const locales = ['en', 'ar'];
export const defaultLocale = 'en';

export const dictionaries = {
  en,
  ar
};